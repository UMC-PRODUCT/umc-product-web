/**
 * Recruiting 도메인 - 진실의 원천 (Source of Truth)
 *
 * 리크루팅 관련 모든 타입과 상수는 이 모듈에서 export합니다.
 * 다른 곳에서 리크루팅 관련 타입이 필요하면 이 모듈에서 import하세요.
 *
 * @example
 * import type { PartData, CurriculumItem } from '@features/recruiting/domain'
 * import { PART_REQUIRED_SKILL } from '@features/recruiting/domain'
 */

export * from './constants'
export * from './model'
