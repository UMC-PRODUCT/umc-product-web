import * as S from './SectionTitles.style'

const SectionSubTitle = ({ subtitle }: { subtitle: string }) => {
  return <S.SectionSubTitleStyle>{subtitle}</S.SectionSubTitleStyle>
}

export default SectionSubTitle
